{
  "name": "mondial-classe",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "netlify dev --dir=public",
    "serve": "npx serve public",
    "check": "node --check netlify/functions/create-room.mjs && node --check netlify/functions/get-room.mjs && node --check netlify/functions/update-scores.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^9.1.2"
  },
  "devDependencies": {
    "netlify-cli": "^22.0.0",
    "serve": "^14.2.5"
  }
}
